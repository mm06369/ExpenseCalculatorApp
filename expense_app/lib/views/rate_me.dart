

import 'package:flutter/material.dart';

class RateMePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Rate Me'),
      ),
      body: Center(
        child: Text('Rate Me Page'),
      ),
    );
  }
}